name: 'workload-credentials'
description: 'A typescript action to get secrets from BeyondTrust workload credentials'
author: BeyondTrust/volt
inputs:
  api-version:
    description: 'The BeyondTrust API version.'
    required: false
    default: '2026-02-16'
  site-id:
    description: 'The BeyondTrust site ID (UUID).'
    required: true
  static-secrets:
    description: 'YAML list of secrets to retrieve. Each entry: path (required), key (optional, omit to export all fields), output-name (optional alias, or prefix if ending with *), export-to-env (optional, default false).'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
